const startFunc = (headConfig) => {
    if (!headConfig) return null;

    const header = document.createElement("div");
    header.textContent = "footer";

    return header;
};

export default startFunc;
